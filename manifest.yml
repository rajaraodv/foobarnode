---
applications:
  .:
    name: foobarnode
    framework:
      name: node
      info:
        mem: 64M
        description: Node.js Application
        exec: !!null 
    url: ${name}.${target-base}
    mem: 64M
    instances: 1
    services:
      mongodb-foobar:
        type: mongodb
